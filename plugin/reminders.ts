export const schedulerReminder = `<internal_reminder>!IMPORTANT! Scheduler workflow: plan lanes/dependencies → dispatch background specialists → track task IDs → wait for hook-driven completion → reconcile terminal results → verify. Do not poll running jobs, consume running-job output, or advance dependent work. If a lane's terminal result is already reconciled and verified but the Background Board still shows it as running, treat that as stale-running board residue rather than active execution. Only then may you cancel it as obsolete cleanup; this is lifecycle cleanup, not rollback. Do not cancel when output is partial, terminal status is unclear, reconciliation is incomplete, verification still depends on the lane, or the same session must continue. !END!</internal_reminder>`

export const conductorWriteBoundaryReminder = `<internal_reminder>!IMPORTANT! In Conductor Mode, writes are limited to safe project-root .ramblings/ artifacts only: .ramblings/plans/**, .ramblings/specs/**, .ramblings/checklists/**, .ramblings/handoffs/**, .ramblings/debug/**, .ramblings/retros/**, and .ramblings/archive/**. Do not write product code, tests, runtime/build config, nested subproject .ramblings/ directories, or external/global .ramblings/ locations. If a requested write falls outside this boundary, refuse and explain that Conductor Mode is planning-only and artifact-scoped. !END!</internal_reminder>`
