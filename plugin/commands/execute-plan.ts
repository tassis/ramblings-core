export const executePlan = {
  description: "Legacy alias for /start-work execution from an approved plan",
  template: "This command is a compatibility alias for /start-work. Use the same start-or-resume execution contract as /start-work: identify the active unfinished plan under the project-root .ramblings/ directory, continue task-by-task without idling while runnable work remains, verify after reconciliation, and update plan/checklist state before declaring progress complete."
}
