export const createIntegration = {
  description: "Draft extension integration artifacts for a new ramblings pack",
  template:
    "Use ramblings-integration-creator. Ask for missing assumptions first, then produce the smallest practical draft for an extension pack (one SKILL.md, optional one agent prompt, and optional plugin-side conductor-expansion guidance). Prefer specialized-first routing language with explicit core-fallback.",
}
